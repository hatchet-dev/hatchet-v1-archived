import { SectionCard } from "@hatchet-dev/hatchet-components";
import styled from "styled-components";

const ModuleSettingsCard = styled(SectionCard)`
  padding: 40px;
  height: fit-content;
`;

export default ModuleSettingsCard;
